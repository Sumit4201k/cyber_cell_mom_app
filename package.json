{
  "name": "cyber-cell-mom-tool",
  "version": "1.0.0",
  "description": "State Cyber Cell Minutes of Meeting & Action Item Tool",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "setup": "setup.bat",
    "setup:mac": "chmod +x setup.sh run.sh && ./setup.sh",
    "start": "concurrently -k -n \"NODE,REACT\" -c \"cyan,magenta\" \"npm run start:backend\" \"npm run start:frontend\"",
    "start:full": "concurrently -k -n \"PYTHON,NODE,REACT\" -c \"yellow,cyan,magenta\" \"npm run start:python\" \"npm run start:backend\" \"npm run start:frontend\"",
    "start:python": "cd python-service && python main.py",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "test": "node backend/test_qa_suite.js",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
