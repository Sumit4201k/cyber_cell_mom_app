{
  "name": "cyber-cell-mom-tool",
  "version": "1.0.0",
  "description": "State Cyber Cell Minutes of Meeting & Action Item Tool",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "setup": "setup.bat",
    "setup:mac": "chmod +x setup.sh run.sh && ./setup.sh",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:full": "concurrently \"npm run start:python\" \"npm run start:backend\" \"npm run start:frontend\"",
    "start:python": "cd python-service && uvicorn main:app --host 0.0.0.0 --port 8000",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
